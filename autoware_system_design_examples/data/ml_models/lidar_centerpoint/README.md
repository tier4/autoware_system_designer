# lidar_centerpoint (dummy bundle)

One directory per model variant; the bundle version is recorded inside `ml_package.param.yaml`.
`legacy/` records an unsupported version and exists to exercise `requires_version`.
