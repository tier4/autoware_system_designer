<?xml version="1.0"?>
<launch>
  <!-- This launch file is auto-generated by the autoware_system_designer -->
  <!-- Deployment variant wrapper -->
  <!-- Deploy: {{ deploy_name }} / Mode: {{ mode_key }} / Compute Unit: {{ compute_unit }} -->

{% for argument in arguments %}
  <arg name="{{ argument.name }}" default="{{ argument.value }}"/>
{% endfor %}

  <include file="{{ base_launcher_path }}">
{% for argument in arguments %}
    <arg name="{{ argument.name }}" value="$(var {{ argument.name }})"/>
{% endfor %}
  </include>
</launch>
