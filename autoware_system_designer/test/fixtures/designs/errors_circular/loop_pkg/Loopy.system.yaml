autoware_system_design_format: 0.4.0

name: Loopy.system

description: System that instantiates a self-recursive module.

modes:
  - name: default
    description: Default mode
    default: true

components:
  - name: loop
    entity: Loop.module
    path: /loop
    compute_unit: main_ecu

connections: []
