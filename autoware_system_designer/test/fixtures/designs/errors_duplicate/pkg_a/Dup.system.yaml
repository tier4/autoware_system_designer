autoware_system_design_format: 0.4.0

name: Dup.system

description: Uses a node name declared by two packages.

modes:
  - name: default
    description: Default mode
    default: true

components:
  - name: echo
    entity: Echo.node
    compute_unit: main_ecu

connections: []
