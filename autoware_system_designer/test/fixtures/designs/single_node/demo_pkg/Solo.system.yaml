autoware_system_design_format: 0.4.0

name: Solo.system

description: Single talker node system.

modes:
  - name: default
    description: Default mode
    default: true

components:
  - name: talker
    entity: Talker.node
    compute_unit: main_ecu

connections: []
